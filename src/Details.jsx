import { Link } from "react-router-dom";

export function Details() {
  return (
    <nav>
      {/* Componente usado para criar links de navegação entre diferentes rotas */}
      <Link to="/">Voltar para Home</Link>
    </nav>
  )
}